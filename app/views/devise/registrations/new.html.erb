
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>



  <div class="container">
    <div class="row">
      <div class="col-md-6 login-box my-5 text-center">
        <h2>Rejoins nous!</h2>
        <%= f.text_field :name, autofocus: true, placeholder: 'Nom Complet', class: "form-control" %>
        <%= f.email_field :email, autofocus: true, placeholder: 'Ton email', class: "form-control" %>
        <%= f.password_field :password, placeholder: 'Mot de passe', class: "form-control"  %>
        <%= f.password_field :password_confirmation, placeholder: 'Confirmation mot de passe', class: "form-control"  %>
        <%= f.submit "Rejoindre", class: 'btn btn-primary' %>

      <% end %>
      <br>
  <br><%= render "devise/shared/links" %>
      </div>
      <div class="col-md-6 my-4">
        <%= image_tag 'login.png', class:"login-img img-fluid" %>
      </div>
    </div>
  </div>
